name: Test building the Docker image

on:
  workflow_call:

jobs:

  build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Download App Image
        uses: actions/download-artifact@v5
        with:
          name: app-image
          path: ./target/request-sink-0.0.4.zip
      - name: Build Docker Image
        run: |
          unzip ./target/request-sink-0.0.4.zip -d ./target/jpackage-image/request-sink/
          ls -al
          ls -al ./target/jpackage-image/request-sink/
          ls -al ./target/jpackage-image/request-sink/bin
          docker build -t request-sink .
      - name: Run Docker Image
        run: docker run --rm -d -p 8080:8080 request-sink
      - name: Wait for request-sink to start
        run: sleep 5
      - name: Stop Docker Container
        run: docker stop request-sink
